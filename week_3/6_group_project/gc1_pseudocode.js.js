// My role in the group is:
// Translate user stories into pseudocode


// Here is my part of the challenge:

/** 
 * Pseudo 1
 * function sum(array A){
 *  if length(A) = 0
 *		return 0
 * 	return A.first + sum(A - [A.first])
 * }
 * 
 */


/**
 * Pseudo 2
 * function mean(array B){
 * 	if length(B) = 0
 * 		return 0
 *	return sum(B)/length(B)
 * }
 */

/**
 * Pseudo 3
 * function median(array C){
 * 	sort(C)
 *	middle = length(C)/2	
 * 	if length(C) is odd
 *		return C[middle+0.5]
 *	else
 *		return (C[middle]+C[middle+1]/2
 * }
 */